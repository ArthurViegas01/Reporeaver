# Frontend env (Vite reads VITE_* at build/dev time).
# Local dev uses the proxy in vite.config.ts so this can stay relative.
VITE_MCP_SERVER_URL=/mcp
VITE_HEALTH_URL=/health
VITE_ENVIRONMENT=development
